# Robin Padilla Simulator (UI scaffold)

This is a minimal Vite + React scaffold for the "Robin Padilla Simulator" browser game UI. No game logic included — structure, navigation, and placeholder UI only.

Install and run:

```bash
npm install
npm run dev
```
